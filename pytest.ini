[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --tb=short
    --strict-markers
    -x
markers =
    unit: 纯单元测试（无外部依赖）
    integration: 集成测试（需要外部服务）
    slow: 耗时较长的测试
asyncio_mode = auto
